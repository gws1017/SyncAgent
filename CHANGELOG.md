# Changelog

이 프로젝트는 PC(Windows)와 Android 두 플랫폼을 하나의 버전 번호로 관리합니다.
새 기능/수정 사항은 실제로 버전을 올리기 전까지 `[Unreleased]` 아래에 먼저 기록하고,
릴리즈 시점에 해당 섹션 제목을 버전 번호로 바꿉니다.

버전 번호는 `src/dashboard.cpp`의 "TEXT RPG vX.Y.Z" 문자열과
`android/app/build.gradle`의 `versionName`을 항상 같이 맞춥니다.
(안드로이드의 `versionCode`는 사람이 보는 버전과 무관한 Play Store 내부 일련번호라
업로드할 때마다 그냥 1씩 증가시키면 됩니다.)

## [Unreleased]

### 수정
- **[버그]** 장비탭처럼 목록이 길어지는 탭에서 스크롤이 잘 안 되던 문제 —
  각 줄에 버튼(장착/리롤/삭제)이 빽빽해서 터치로 드래그할 빈 공간을 찾기
  어려웠음. 처음엔 항상 보이는 세로 스크롤바를 추가하는 식으로 대응했었는데,
  그 스크롤바가 460px 고정 레이아웃 폭을 갉아먹어서 PC에서 체력바 등 오른쪽
  끝 텍스트가 잘리는 새로운 문제가 생김 — 대신 안드로이드에서 화면 아무 데나
  드래그해도 스크롤되도록 터치 제스처 자체를 고쳐서(아래 항목) 스크롤바는
  기본(필요할 때만 자동으로 뜨는) 상태로 되돌림.
- 안드로이드에서 버튼 위를 드래그해도 스크롤이 되도록 개선 — 기존엔 터치를
  그냥 마우스 클릭으로만 매핑해서, 버튼이 있는 자리를 드래그하면 스크롤 대신
  그 버튼이 눌려버렸음. 이제 터치 시작 후 일정 거리(8dp) 이상 움직이면 그때
  스크롤로 판정해서 휠 이벤트로 전환하고(버튼 눌림 취소), 안 움직이고 뗐으면
  그 자리에서 탭(클릭)으로 처리 — 화면 어디를 드래그해도 스크롤되게 함.
- **[버그]** 위 스크롤 방향이 반대로 느껴지던 문제 — 손가락을 위로 드래그하면
  터치스크린 표준대로 콘텐츠가 손가락을 따라 올라가서(아래쪽 내용이 보임)
  야 하는데 반대로 동작했음. 휠 이벤트 부호를 수정.

### 추가
- **[안드로이드 전용]** 리워드 광고 기능 2종 (`docs/design_backlog.md` 설계 반영)
  - 자동합성 버프: 광고 시청 시 3시간 동안 매 틱 자동으로 합성 실행(등급 낮은
    순, 등급당 최대 1회/틱). 성공률/결과는 수동 합성과 완전히 동일 — 확률
    자체를 건드리는 게 아니라 매번 손으로 안 눌러도 되는 편의 기능. 재시청
    시 잔여시간 리필(3시간으로 리셋, 스택 안 됨).
  - 보관함 확장: 광고 시청마다 영구로 +5칸(최대 3회, 총 +15칸). (2026-07-24
    실사용 테스트 피드백으로 +2칸/5회 → +5칸/3회 상향 조정)
  - PC는 이 두 기능을 얻는 버튼 자체가 없음 — 세이브가 모바일과 동기화되는
    구조라 PC에 공짜 획득 경로를 주면 모바일 광고가 무의미해짐. 모바일에서
    정당하게 얻은 버프/확장은 세이브 동기화로 PC에서도 정상 적용됨.
  - AdMob SDK 신규 연동(`play-services-ads`), 실제 발급받은 앱 ID/광고 단위 ID
    반영 완료(테스트 ID 아님). 실기기에서 동작 확인함.
- Google 계정 연동 (PC/안드로이드 둘 다) — 클라우드 동기화(코드 기반)를 계정
  로그인으로 자동화. 연동하면 Google 계정 고유 ID를 동기화 코드로 그대로 써서
  코드를 직접 만들고 입력할 필요 없이 자동 업로드/다운로드됨(2분 주기 자동
  업로드, 연동 시점엔 즉시 동기화). 기존 계정으로 이미 저장된 세이브가 있으면
  그걸로 교체, 없으면 지금 로컬 세이브를 그대로 올림. 최초 1회 연동 성공 시
  보관함 +5칸 보상(광고 확장 한도와 별개 — 재연동해도 중복 지급 안 됨).
  - 안드로이드: `play-services-auth` SDK로 앱 안에서 바로 로그인.
  - PC: 표준 "설치형 앱" OAuth 루프백 플로우(RFC 8252) 직접 구현
    (`google_auth_win.cpp`) — 시스템 브라우저를 열어 로그인시키고, 127.0.0.1의
    임시 포트에 띄운 로컬 서버로 인증 코드를 받아 토큰 교환. 안드로이드처럼
    SDK 하나로 되는 게 아니라 Google Cloud Console에 별도 "데스크톱 앱" OAuth
    클라이언트를 새로 발급받아야 함.
  - 둘 다 기존 코드 기반 동기화 백엔드(Firebase, 익명 인증)를 그대로 재사용 —
    Firebase DB 규칙이 "인증만 되면"(익명도 인정) 아무 코드나 읽고 쓸 수 있게
    돼 있어서, 별도 서버 인증 교환 없이 계정 고유 ID를 코드로 바로 씀.

### 기타
- Firebase 프로젝트 계정 문제로 새 프로젝트(`syncagent-20804`)로 이전 — API
  키/DB 호스트를 PC(`cloud_sync_win.cpp`)와 안드로이드(`CloudSync.kt`) 양쪽에
  반영. 기존 코드 기반 클라우드 동기화 유저는 새 프로젝트 세팅(익명 인증 활성화,
  DB 규칙) 완료 후 정상 동작.

### 밸런스
- 2차 특성 9개 전면 재설계 — 1차와 계수만 맞춘 "그냥 더 센 버전"에서, 이름
  컨셉에 맞는 트레이드오프형으로 변경.
  - 전사: 불굴의 의지(체력 50% 이하일 때만 방어력↑, 조건부 발동), 광전사(공격력↑/받는피해↑),
    수호자(방어력↑/공격속도↓)
  - 마법사: 마나 코어(폭발확률↑/폭발데미지↓), 원소 폭발(폭발데미지↑/평타데미지↓),
    라이프 드레인(체력흡수↑/공격력↓, 기존 "흡혼"에서 개명)
  - 도적: 쾌속(공격속도↑/방어력↓), 치명적 기습(방어관통 신규 스탯/회피율↓, 기존 "추가공격확률"에서
    변경 — 1차 "기습"과 메커니즘이 중복이었음), 소매치기(공격 성공 시 확률적 골드 획득, 실패 시
    자기 체력 소량 피해 — 기존 "야성"을 확률형 특성으로 완전히 교체)
  - 상세 수치는 `docs/design_system.md` 참고

## [1.1.0] - 2026-07-24

### 추가
- 장비 삭제 / 리롤 기능 — 쓸모없는 아이템(특히 낮은 옵션의 전설 등급)이 인벤토리
  칸만 차지하던 문제 대응. 삭제는 즉시 버림, 리롤은 골드를 내고 스탯 종류만
  다시 뽑음(등급/보너스%는 유지). 리롤 비용은 등급이 높을수록 커짐.
  (비공개 테스트 피드백 #4)
- 업그레이드 탭에 골드/경험치/드랍률 현재 배율을 업글·계승·장비 기여분으로
  분해해서 보여주는 표시 추가 (비공개 테스트 피드백 #2)
- 최초 실행 시 영웅 선택 화면에 "이 게임은 방치형 RPG" 온보딩 설명 추가
  (비공개 테스트 피드백 #1)
- 체력흡수(라이프스틸) 발생 시 회복량이 고정 텍스트로 묻혀서 안 보인다는 피드백
  대응 — 새로 회복이 발생한 틱마다 2초간 밝게 떴다가 옅어지는 팝업으로 표시
  (비공개 테스트 피드백 #3)

### 수정
- **[버그]** 프레스티지 계승 보너스(legacyBonusPct)가 스탯 계산에서 100배로
  과적용되던 문제. 37.5% 보너스가 "1.375배"가 아니라 "38.5배"로 들어가서
  프레스티지 한 번에 체력/방어력/공격력이 수십 배씩 폭증했음.
  (비공개 테스트 피드백 #9)
- PC 트레이 / 안드로이드 상단 상태바의 "작은 아이콘"이 기본(비프라이버시) 상태에서
  여전히 검 모양이었던 문제 — 런처/배지용으로 이미 바꿔둔 마름모(젬) 디자인과
  안 맞아서 통일감이 없었음. 기본 아이콘을 마름모로 교체(PC `app_fantasy.ico`,
  안드로이드 `ic_diamond_notif.png` 신규 추가), 프라이버시 모드일 때의 sync
  아이콘(원형)은 그대로 유지 — 마름모 ↔ 원형으로 토글되도록 통일.
- PC는 스토어 심사 정책이 없는 사이드로드 배포라 안드로이드처럼 "기본은 정직하게"를
  강제할 필요가 없음 — 신규 설치 시 프라이버시 모드가 곧바로 켜진 상태로 시작하도록
  변경(트레이 아이콘도 처음부터 sync/원형으로 표시). 세이브 파일이 있는 기존 유저는
  저장된 값을 그대로 불러오므로 영향 없음. 안드로이드는 정책상 기본 꺼짐 그대로 유지.
- 장비탭 삭제/리롤 규칙 정비 (실사용 테스트 피드백)
  - 삭제 버튼은 전설 등급에만 노출 — 낮은 등급은 합성으로 자연히 소모되니
    불필요하고, 합성 막장인 전설 등급만 스탯이 안 맞으면 영구히 칸을 차지함.
  - 리롤은 영웅 등급 이상부터, 아이템당 평생 한 번만 가능 — 무제한이면
    사실상 원하는 스탯 확정 뽑기가 되어버리는 문제 방지.
  - 삭제는 확인창 없이 즉시 처리(여러 개 정리할 때 매번 확인받으면 번거로움)
    되므로, 실수 클릭 방지를 위해 장착/리롤 버튼과 줄을 띄워 오른쪽 끝에
    멀리 배치.

### 밸런스
- 2차 특성이 1차보다 계수가 20~40% 낮게 설계되어 있어 "2차 투자가 손해"로
  느껴지던 문제 — 매칭되는 스탯 계수를 1차와 동일하게 맞춤 (체력 보너스는
  2차 고유 혜택으로 유지). (비공개 테스트 피드백 #5)
- 초반 생존력 보강 — 기본 최대체력 시작값을 200 → 350으로 상향 (스테이지당
  성장률은 그대로 유지, 초반 쿠션만 확보). (비공개 테스트 피드백 #6)
- 인벤토리가 가득 찬 상태에서 드랍 롤이 될 때마다 반복해서 뜨던 "가방 가득 참"
  푸시 알림 제거 — 대시보드(최근 이벤트)에는 계속 표시되지만 시스템 알림은
  더 이상 안 보냄. (비공개 테스트 피드백 #7)
- 레벨업 경험치 요구량 지수를 1.30 → 1.25로 완화 — 기존 곡선은 2차 특성을
  다 채우는 레벨 40 도달까지 약 20시간이 걸려 "떡벽"으로 느껴졌음. 1.25로
  낮추면 레벨 40이 약 4시간대로 줄어들어 하루 안에 첫 프레스티지가 현실적인
  목표가 되면서도, 후반(레벨 50+)은 여전히 오래 걸리는 지수 곡선을 유지함.
  (비공개 테스트 피드백 #8)
- 도적 "기습/치명적 기습" 특성이 이미 공속 배수가 적용된 전체 데미지를 통째로
  ×2 하던 방식 → "한 타 분량"만 추가로 더하는 방식으로 변경. 공속(타수 증가)과
  기습 확률이 곱연산으로 겹쳐서 도적만 딜이 눈덩이처럼 불어나던 구조적 불균형
  수정. (비공개 테스트 피드백 #12)

### UI
- 장비탭에서 리롤/삭제 버튼이 아이템 정보와 같은 줄에 있어 PC 창 폭(460)을
  넘어가 잘리던 문제 — 버튼을 다음 줄로 내려서 항상 폭 안에 들어오게 수정.
- **[버그]** 체력흡수(라이프스틸) 회복량이 정수 HP로 버림 처리되면서 데미지가
  작을 때(예: 14뎀 x 3% = 0.42) 매번 0으로 사라지던 문제 — 크리티컬/폭발
  데미지처럼 큰 값이 아니면 회복이 거의 안 되는 것처럼 보였음. 버려지는
  나머지를 `lifestealCarry`에 이월해서 누적되면 반영되도록 수정(내 체력
  표시 자체는 정수로 유지). 회복 팝업(초록 글씨)은 정수로 반영된 양이 아니라 이번 틱에
  실제 계산된 소숫점 값(`lastHealFrac`)을 그대로 보여주도록 변경 —
  0.42처럼 작은 값도 매 틱 계속 뜨는 걸 확인할 수 있음.

## [1.0.1] - 이전 릴리즈

- 멀티 히어로 로스터, 계승 프레스티지 시스템
- 안드로이드 포팅 (백그라운드 실행, 알림, 홈화면 위젯, 프라이버시 모드)
- PC ↔ 모바일 Firebase 클라우드 세이브 동기화
- 설정 탭 신설 (언어 / 백그라운드 실행 / 프라이버시 모드 / 클라우드 동기화)
